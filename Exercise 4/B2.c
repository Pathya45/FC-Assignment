#include<stdio.h>
int calculatesum(int n);
{
	int sum=0;
	while(sum)
}
void main()
{
	int n,result;
	printf("\nenter the numbers ");
	scanf("%d",&n);
	result=calculatesum(n);
	printf("sum of digit %d and digit %d",sum,digit);
}

	
